# Local API
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production (used automatically by `next build` / Vercel via .env.production)
# NEXT_PUBLIC_API_URL=https://virtual-signals-backend.onrender.com

# Keep false so users must wait for admin payment confirmation.
NEXT_PUBLIC_OPEN_ACCESS=false
